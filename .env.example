# Server API (local tanpa Docker)
PORT=3000

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=fintrack
DB_USER=audi
DB_PASSWORD=fintrack_secret

# Docker Compose (opsional override port host)
API_PORT=3000
WEB_PORT=5173
